# Lanes

**Nothing below the marker is authoritative, and none of it is hand-written.**
Regenerate it:

    .venv\Scripts\python.exe scripts/lane_board.py --write

Run that before you claim an ADR number or a schema version, and again before
you push. It is a snapshot, not a lock -- see the script's docstring and
`docs/adr/README.md`.

## Slow facts

- Lane worktrees are **Herdr's**, not any session's. Nothing in this repo starts
  or stops one; `git worktree list` is the only truth about which exist.
- `tasks/LANES.md` is **integrator-only**, in the spirit of ADR 0003 §2. A lane
  regenerating it recreates the append-conflict that section exists to prevent.
- On 2026-08-27 two lanes collided three times in one day, on ADR ordinals and
  on `SCHEMA_VERSION`. Two of the three merged **cleanly**, because two lanes
  writing `0077-a.md` and `0077-b.md` have no line to conflict on. Guard:
  `tests/test_parallel_lanes_do_not_collide.py`. Rule: `docs/adr/README.md`.

- **How to run a lane day to day is ADR 0003 §6** — commit early, keep
  lanes short, split at the decision boundary, close on merge, one
  integrator, two lanes is comfortable. Stated once there, pointed at
  from here, so the two cannot disagree.

## Allocation ledger

The only part a measurement cannot produce, because it is a claim about the
*future*: what a live lane has said it will take, before it exists on disk.
Add a row when you claim; close it with the merge sha when it lands.

| lane | claims | stated | closed by |
|---|---|---|---|
| ~~`parlay-props`~~ | ~~an ADR number, at merge~~ | 2026-08-27 | took **0079** at the boundary; merged `ba8fc0d` |

<!-- BOARD BELOW -- regenerated by scripts/lane_board.py; do not hand-edit -->

```
# lane board -- 2026-08-27T19:03:01Z
# repo       C:/Users/josep/Documents/Claude/Projects/kalshi_betting_tool
# lane root  C:/Users/josep/.herdr/worktrees/kalshi_betting_tool
# Generated by `scripts/lane_board.py`. Do not hand-edit; regenerate.
# A snapshot, not a lock. It reserves nothing and does not fix the
# ordinal counters -- see the script docstring and tasks/lessons.md:168-191.

LANES
-----
kalshi_betting_tool       e38a82b  main  (worktree)
                              path      C:/Users/josep/Documents/Claude/Projects/kalshi_betting_tool
                              vs origin 0 unpushed, 0 unpulled
                              dirty     3 files, 200 lines
                                 M      91  tests/test_lane_board.py
                                 M      68  scripts/lane_board.py
                                 M      41  tasks/lessons.md
                              adr       baseline: 79 numbers on main, highest 0079
                              schema    v24  CLAIMED

parlay-props              e90b154  parlay_props  (worktree)
                              path      C:/Users/josep/.herdr/worktrees/kalshi_betting_tool/parlay-props
                              vs main   0 ahead, 2 behind   merge-base e90b154
                              dirty     clean
                              adr       no unmerged claim
                              schema    v24  INHERITED -- backend/store/db.py untouched since the merge-base, so this is not a claim

hedging_research          f59d102  hedging_research  (branch)
                              vs main   0 ahead, 18 behind   merge-base f59d102
                              dirty     clean
                              adr       no unmerged claim
                              schema    v24  INHERITED -- backend/store/db.py untouched since the merge-base, so this is not a claim

ui-work                   a1ae05e  ui-work  (branch)
                              vs main   0 ahead, 313 behind   merge-base a1ae05e
                              dirty     clean
                              adr       no unmerged claim
                              schema    v9  INHERITED -- backend/store/db.py untouched since the merge-base, so this is not a claim

hedging-research          UNREGISTERED DIRECTORY
                              present on disk at C:\Users\josep\.herdr\worktrees\kalshi_betting_tool\hedging-research, absent from `git worktree list`, and carrying no `.git` of its own -- so it looks like the shell a merged lane leaves behind. `git worktree prune` does not remove these. Nothing can be established about its contents from here; check before removing it.

FINDINGS
--------
UNREADABLE lane   hedging-research
                  present on disk at C:\Users\josep\.herdr\worktrees\kalshi_betting_tool\hedging-research, absent from `git worktree list`, and carrying no `.git` of its own -- so it looks like the shell a merged lane leaves behind. `git worktree prune` does not remove these. Nothing can be established about its contents from here; check before removing it.

VERDICT  COLLISIONS 0   OVERLAPS 0   UNREADABLE 1   LANES COMPARED 3   ->  exit 3 (UNREADABLE: this is not a clean bill)
```
