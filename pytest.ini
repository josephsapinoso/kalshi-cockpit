[pytest]
# Async tests need no marker.
asyncio_mode = auto
testpaths = tests
# Shared fixtures live in conftest.py at the repo root, not in tests/, because
# backend/ is imported as a package from the root.
